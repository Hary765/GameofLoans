<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.harpatel.gameofloans">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PrivateLoanActivity" />
        <activity android:name=".SubsidizedActivity" />
        <activity android:name=".UnsubsidizedActivity" />
        <activity android:name=".SubsidizedFinalActivity" />
        <activity android:name=".UnsubsidizedFinalActivity" />
        <activity android:name=".PrivateFinalActivity" />
        <activity android:name=".DefinitionsActivity" />
        <activity android:name=".BudgetingActivity" />
        <activity android:name=".CalculationsActivity" />
        <activity android:name=".PaymentPlanActivity"></activity>
    </application>

</manifest>